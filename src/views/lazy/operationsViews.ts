/**
 * Operations/utility view group (M11/I6 route splitting).
 *
 * One lazy chunk for the low-frequency surfaces. Re-exports only; the views
 * themselves are untouched.
 */
export { FinanceView } from "../Finance";
export { ReportsView } from "../Reports";
export { MessagesView } from "../Messages";
export { LibraryView } from "../Library";
export { GalleryView } from "../Gallery";
export { SettingsView } from "../Settings";
