# Data source: "demo" (localStorage DemoStore, default) or "api" (real REST backend)
VITE_DATA_SOURCE=demo

# Base URL of the /api/v1 REST backend. Used only when VITE_DATA_SOURCE=api.
VITE_API_BASE_URL=/api/v1
